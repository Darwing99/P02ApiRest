<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="P02ApiRest.MainPage">
    <StackLayout Padding="20" >
        <Button Visual="Material" Text="Country"
                Clicked="OnButtonClicked" />
        <CollectionView HorizontalScrollBarVisibility="Default" VerticalScrollBarVisibility="Default"  SelectedItem=""   x:Name="collectionView">
            <CollectionView.ItemTemplate  >
                <DataTemplate   >
                   
                    <Frame  CornerRadius="5" HasShadow="True">
                        <StackLayout > 
                            
                         <Grid >
                                <Label Grid.Row="0"  Grid.Column="0" Text="{Binding name}"
                               FontSize="Medium" />
                                <Label  Grid.Row="0" Grid.Column="1" Text="{Binding subregion}"
                               TextColor="black"
                               FontSize="Small" />
                        
                                <Label  Grid.Row="0"  Grid.Column="2" Text="{Binding population}"
                                TextColor="black"
                                FontSize="Small" />
                              
                                <Label  Grid.Row="0" Grid.Column="3" Text="{Binding capital}"
                                TextColor="black"
                                FontSize="Caption" />
                               

                            </Grid>
                        
                        
                        </StackLayout>
                    </Frame>
                   
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>